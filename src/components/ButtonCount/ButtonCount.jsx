import Button from 'react-bootstrap/Button';

export const ButtonCount = ({handleInter}) => {
    return (

        <Button onClick= {handleInter} variant="primary">Agregar al carrito</Button>

    )
}